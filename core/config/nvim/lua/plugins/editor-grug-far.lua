
-- Search and replace tool featuring interactive multi-file substitutions.
-- Uses ripgrep under the hood for fast text patterns scanning.
-- Includes visual result previews, filters, and command setups.

return {
	"Magicduck/grug-far.nvim",
	cmd = "GrugFar",
	opts = { headerMaxWidth = 80 },
}
