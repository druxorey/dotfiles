#!/bin/bash

function main() {
	printf "The commit: "
	git log --oneline --graph --decorate -n 1
	git reset --soft HEAD~1
	printf "\nWas restarted to previous commit: "
	git log --oneline --graph --decorate -n 1
}

main "$@"
